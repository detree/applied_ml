There are four .R files included in the folder, which are corresponding to four parts in the problem 2.1

To run the code, a R library is needed, which can be found on the https://www.r-project.org/. After that, klaR and caret library needs to be installed as well. RStudio may also needed which can be found on https://www.rstudio.com/.

To run each .R file, the working path need to be changed first in the setwd() function. Additionally, for part d, SVMlight is needed and can be found on svmlight.joachims.org/. After downloading the binary file, SVM’s path need to be changed in the svmlight() function.
